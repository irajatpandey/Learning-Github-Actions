name: python-Pipeline

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs: 
  ci-pipeline-jobs:
    uses: irajatpandey/reusable-workflow/.github/workflows/ci-pipeline.yml@main
    secrets: 
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}